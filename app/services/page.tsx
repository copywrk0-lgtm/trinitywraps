import Link from "next/link";
import PageHero from "../../components/PageHero";
import { services, WHATSAPP_URL } from "../../lib/site";

export const metadata={title:"Services"};
const process=[['01','Tell us the vehicle','Send the model, current finish and what you want to change.'],['02','Choose the direction','We align on colour, finish, graphics, protection and scope.'],['03','Get your quote','You receive a clear project quote before the work starts.'],['04','Bring it in','We prepare, apply, finish and inspect the vehicle before handover.']];
export default function Services(){return <main><PageHero kicker="01 / SERVICES" title="BUILT FOR" accent="TRANSFORMATION." copy="Wraps, graphics, protection and detailing for cars and bikes — all under one roof in Vijayawada."/><section className="shell service-list section-pad compact-top">{services.map((s)=><article key={s.no}><span>{s.no}</span><div><h2>{s.title}</h2><p>{s.text}</p></div><a href={`${WHATSAPP_URL}?text=${encodeURIComponent('Hi Trinity Wraps, I want to know more about '+s.title)}`} target="_blank" rel="noreferrer">Ask about this ↗</a></article>)}</section><section className="process section-pad"><div className="shell"><div className="section-heading split-heading"><div><div className="section-kicker">HOW IT WORKS</div><h2>FROM IDEA<br/><em>TO ROAD.</em></h2></div><p>No complicated booking flow. Start on WhatsApp, share your vehicle and idea, and we&apos;ll guide the next step.</p></div><div className="process-grid">{process.map(p=><div key={p[0]}><span>{p[0]}</span><h3>{p[1]}</h3><p>{p[2]}</p></div>)}</div><Link className="button primary" href="/contact">Start your project <span>↗</span></Link></div></section></main>}
