"use client"; import { useState } from "react"; import { finishes } from "../../data/finishes";
export default function FinishSelector(){const [active,setActive]=useState(0);return <section className="finish-section section-pad"><div className="shell finish-grid"><div><div className="section-kicker">03 / CHOOSE A DIRECTION</div><h2>WHAT SHOULD<br/><em>IT FEEL LIKE?</em></h2><p className="finish-intro">You do not need to know the vinyl code. Start with the character you want, then refine the finish with the studio.</p><div className="finish-tabs">{finishes.map((f,i)=><button key={f.name} className={active===i?"active":""} onClick={()=>setActive(i)}>{f.name}</button>)}</div><p className="finish-note"><b>{finishes[active].name}</b> — {finishes[active].note}</p></div><div className={`finish-visual finish-${active}`}><img src="/images/trinity_wraps_02_ktm.jpg" alt="Wrapped motorcycle finish reference"/><div className="finish-label">{finishes[active].name}</div></div></div></section>}
