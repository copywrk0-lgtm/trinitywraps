import Link from "next/link"; import { services } from "../../data/services"; import ServiceCard from "../services/ServiceCard"; import Arrow from "../ui/Arrow";
export default function ServicesSection(){return <section className="services-home section-pad"><div className="shell"><div className="section-heading split-heading"><div><div className="section-kicker">02 / SERVICES</div><h2>BUILT TO<br/><em>TRANSFORM.</em></h2></div><p>From a clean colour change to a full visual makeover, we cover the details that make a vehicle feel finished.</p></div><div className="service-grid">{services.slice(0,8).map(s=><ServiceCard key={s.no} service={s}/>)}</div><div className="section-end"><Link className="text-link" href="/services">Explore all services <Arrow/></Link></div></div></section>}
