"use client"; import { useState } from "react"; import { faqs } from "../../data/faqs";
export default function FAQSection(){const [open,setOpen]=useState(0);return <section className="faq-section section-pad"><div className="shell faq-layout"><div><div className="section-kicker">06 / QUESTIONS</div><h2>BEFORE YOU<br/><em>WRAP IT.</em></h2></div><div className="faq-list">{faqs.map((f,i)=><button key={f[0]} className={`faq-item ${open===i?"open":""}`} onClick={()=>setOpen(open===i?-1:i)} aria-expanded={open===i}><span><b>0{i+1}</b>{f[0]}</span><i>{open===i?"−":"+"}</i>{open===i&&<p>{f[1]}</p>}</button>)}</div></div></section>}
